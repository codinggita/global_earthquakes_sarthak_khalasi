{
  "name": "global-earthquakes",
  "version": "1.0.0",
  "description": "Global Earthquake API and Analysis Project",
  "scripts": {
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "seed": "npm run seed --prefix backend"
  }
}
